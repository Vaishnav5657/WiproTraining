import { FahrenheitePipe } from './fahrenheite-pipe';

describe('FahrenheitePipe', () => {
  it('create an instance', () => {
    const pipe = new FahrenheitePipe();
    expect(pipe).toBeTruthy();
  });
});
