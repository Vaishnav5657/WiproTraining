package com.wipro.basic;

public class Largestarray {

    public static void main(String[] args) {
		/*
		 * int[] numbers = {100, 67, 98, 678, 45, 123};
		 * 
		 * int largest = numbers[0]; // Assume first element is the largest
		 * 
		 * for (int i = 1; i < numbers.length; i++) { if (numbers[i] > largest) {
		 * largest = numbers[i]; // Update if current is greater } }
		 * 
		 * System.out.println("The largest number is: " + largest);
		 */
    	
    	 int[] arr = {10, 5, 20, 8, 15};
    	 int highestnum = 0;
    	 int secondhighestnum = 0;
    	 
    	 for(int i=0; i<arr.length; i++);
    	 	if(arr[i])

}
